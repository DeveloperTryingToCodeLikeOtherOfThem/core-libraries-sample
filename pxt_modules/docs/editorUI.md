# pxt_modules/docs/editorUI

Using this extension 
if you know we have properties like this in blocks from the core libraries here
``` blocks 
let s = Math.sqrt(0)
```
I mean that dropdown menu is the thing I mean if your in the editor in blocks and click that down arrow you see it will put bunch of properties 
so this is what new properties we will make
